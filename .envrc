# 自动激活 Python 虚拟环境
# 使用 direnv 或手动 source 此文件来激活虚拟环境

# 检查虚拟环境是否存在
if [ -d "venv" ]; then
    # 激活虚拟环境
    source venv/bin/activate
    
    # 显示激活状态
    echo "🐍 Python 虚拟环境已激活: $(python --version)"
    echo "📍 虚拟环境路径: $(which python)"
else
    echo "❌ 虚拟环境不存在，请先运行: python -m venv venv"
fi
